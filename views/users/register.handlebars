<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/image/backgrounds/register-image.jpg'); /* Substitua pelo caminho da sua imagem */
            background-size: 100% 100%; /* Ajuste a largura e a altura da imagem */
            background-repeat: no-repeat;
            background-position: center 20px; /* Ajuste para subir a imagem */
            height: 100vh;
            margin: 0;
            padding: 0;
        }
        .card-custom {
            width: 480px; /* Ajuste o tamanho do card conforme necessário */
            height: 500px;
            background-color: #1E3A8A; /* Azul escuro para o fundo do card */
            color: white; /* Texto branco no card */
            border: none;
            position: absolute;
            left: 50%;   /*Ajuste a posição à esquerda */
            top: 50%; /* Centralizar verticalmente */
            transform: translate(-50%, -45%); /* Ajusta a posição horizontal/eixo x e verticalmente/eixoy */
            padding: 0px; /*Define os espaçamento entre o conteúdo e as bordas*/
            text-align: center; /* Centralizar conteúdo */
        }
        .btn-custom {
            background-color: #3B82F6; /* Fundo azul claro para o botão */
            color: white; /* Texto branco no botão */
            border: 1px solid #60A5FA; /* Bordo azul mais claro */
            margin-top: 10px; /* Espaçamento acima do botão */
        }
        .btn-custom:hover {
            background-color: #60A5FA; /* Fundo azul mais claro ao passar o mouse */
            border-color: #3B82F6; /* Bordo azul mais escuro ao passar o mouse */
        }
        .form-label-custom {
            text-align: left; /* Alinha os rótulos à esquerda */
            display: block; /* Garante que cada rótulo fique em uma linha */
            margin-bottom: 5px; /* Espaçamento abaixo de cada rótulo */
        }
        .form-margin { /*Serve para controlar a largura do form dentro do container. Nos ajudou a resolver o problema dos p estarem fora do container nas configurações atuais*/
            margin-bottom: 0px;
        }
    </style>
</head>
<body> <!--Tudo o que é para aparecer na página deve estar dentro deste tag-->
    <div class="container mt-4">
        {{#each errors}} 
            <div class="alert alert-danger">{{text}}</div>
        {{else}}

        {{/each}}
    </div>    
    <div class="card card-custom">
        <div class="card-body">
            <h2>Cadastre-se</h2>
            <p>Seja bem-vindo, caro(a) Biker. Vamos rodar!</p>
            <form action="" method="POST" class="text-center form-margin">
                <div class="mb-3"> <!--Ta garantindo um espaço entre o labels e o botão. Além de compactá-los melhor dentro do card-->
                    <label for="name" class="form-label-custom">Nome: </label>
                        <input type="text" name="name" placeholder="Digite seu nome" class="form-control" required>
                    <label for="email" class="form-label-custom">E-mail: </label>
                        <input type="email" name="email" class="form-control" placeholder="Digite seu e-mail" required>
                    <label for="password" class="form-label-custom">Senha: </label>
                        <input type="password" name="password" class="form-control" placeholder="Digite sua senha" required>
                    <label for="password2" class="form-label-custom">Repita sua senha: </label>
                        <input type="password" name="password2" class="form-control" placeholder="Repita sua senha" required>    
                </div>
                <button type="submit" class="btn btn-custom">Criar conta</button>
            </form>
            <p>ou</p>
            <p>Já possui conta? Clique <a href="/usuarios/login" class="text-decoration-none"><strong>AQUI</strong></a> para entrar</p>    
        </div>
    </div>
</body>
</html>
<!-- Primeira versão
{{#each errors}} 
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h2>Registre-se</h2>
<hr>
<h3>Será um prazer tê-lo aqui, caro Biker!</h3>
<div class="card">
    <div class="card-body">
        <form action="" method="POST"> lembre-se que a estratégia de não definir o action é para o caso da post ter o mesmo nome que a rota get que chamou o formulário HTML no qual estamos.
            <label for="name">Nome: </label>
                <input type="text" name="name" id=placeholder="Digite o nome" class="form-control" required>
            <label for="email">E-mail: </label>
                <input type="email" name="email" class="form-control" placeholder="Digite o e-mail" required>
            <label for="password">Senha: </label>
                <input type="password" name="password" class="form-control" placeholder="Digite a senha" required>
            <label for="password2">Repita sua senha: </label>
                <input type="password" name="password2" class="form-control" placeholder="Repita a senha" required>    
            <button type="submit" class="btn btn-success mt-4">Registrar-se</button>
        </form>
    </div>
</div>-->