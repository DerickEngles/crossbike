{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div> 
{{else}}

{{/each}}
<h3>Entre em Contato!</h3>
<!--quanto mais divs, mais enquadrado fica o nosso template. É como adicionar frames/molduras-->
<div class="card">
    <div class="card-body">
        <form action="" method="POST"> <!--rota post que vamos criar em admin - rota tem sempre a / na frente-->
        <label for="name">Nome completo: </label> <!--for indica qual input este label está associado. Nesse caso, um que tenhamos o input de id "nome" logo abaixo-->
            <input type="text" id="name" name="name" placeholder="Nome completo" class="form-control" required>
        <label for="email">E-mail: </label>
            <input type="email" id="email" name="email" placeholder="Digite seu e-mail" class="form-control">
        <label for="message">Mensagem: </label>
            <textarea type="text" id="message" name="message" placeholder="Digite sua mensagem" class="form-control" required></textarea>
        <br>
        <button type="submit" class="btn btn-success">Enviar mensagem</button>
        </form>
    </div>
</div>